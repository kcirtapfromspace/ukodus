// Runtime config — overwritten at container startup by docker-entrypoint.
// For local dev, set values here or use an .env file.
window.__RUNTIME_CONFIG__ = {
  POSTHOG_KEY: "",
  POSTHOG_HOST: ""
};
